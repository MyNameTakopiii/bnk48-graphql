import type { Member } from "../../types.ts";
import { buildImageUrl } from "../../types.ts";

/**
 * CGM48 1st generation — all members have graduated.
 * Kept as an empty array for historical completeness.
 */
export const cgmGen1: Member[] = [];
